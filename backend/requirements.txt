# ─────────────────────────────────────────────────────────────────
# All packages below have pre-built wheels for:
#   Python 3.11, 3.12, 3.13
#   Windows x64, macOS (Intel + Apple Silicon), Linux x64
#
# No C compiler required. Plain pip install -r requirements.txt works.
# ─────────────────────────────────────────────────────────────────

# Django core
Django==5.1.4
djangorestframework==3.15.2
django-cors-headers==4.4.0
django-filter==24.3

# Image handling — pre-built wheels for all platforms/versions above
Pillow==11.1.0

# Auth
PyJWT==2.10.1

# PostgreSQL driver — binary wheel, no compilation needed
psycopg2-binary==2.9.10

# WSGI server (Linux/macOS only — not used locally on Windows)
gunicorn==23.0.0
